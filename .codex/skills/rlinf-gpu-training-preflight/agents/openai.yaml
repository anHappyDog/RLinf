interface:
  display_name: "RLinf GPU Training Preflight"
  short_description: "Validate RLinf GPU training before long runs"
  default_prompt: "Use $rlinf-gpu-training-preflight to validate this run and hand me exact start, observe, stop, and resume commands."
